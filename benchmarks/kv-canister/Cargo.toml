[package]
name = "ic-sqlite-vfs-kv-bench"
version = "0.1.2"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
candid = "0.10"
ic-cdk = "0.20.1"
ic-sqlite-vfs = { path = "../.." }
serde = { version = "1", features = ["derive"] }
