[package]
name = "minimal-kv"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
candid = "0.10"
ic-cdk = "0.20.1"
ic-sqlite-vfs = { path = "../..", default-features = false, features = ["sqlite-precompiled"] }
ic-stable-structures = "0.7"
